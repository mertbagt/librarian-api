CREATE TABLE libra_checks (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    patron_id INTEGER
        REFERENCES libra_patrons(id) ON DELETE CASCADE NOT NULL,
    book_id INTEGER
        REFERENCES libra_books(id) ON DELETE CASCADE NOT NULL 
);